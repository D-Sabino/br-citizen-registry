interface TogglePanelProps {
    isActive: boolean;
    onToggle: () => void;
}

const TogglePanel = ({
    isActive,
    onToggle
}: TogglePanelProps) => {
    return (
        <div>
            {/* painel */ }
        </div>
    );
};

export default TogglePanel;